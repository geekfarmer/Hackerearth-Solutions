#include<iostream>

using namespace std;

struct node{
    int data;
    struct node * left;
    struct node *right;
};

strcut node* newnode(int data){
    struct node* node (struct node*)malloc(sizeof(strcut node));

    node->data = dat;
    node->left = NULL;
    node->right=NULL;
    return(node);
}

int main(){
    return 0;
}
